# 荣誉准则
在完成本次实验的过程（含此前学习的过程）中，我曾分别与 以下各位 就（与本次实验相关的）以下方面做过交流，还在代码中对应的位置以注释形式记录了具体的交流对象及内容：

《你交流的对象说明》

此外，我也参考了 以下资料 ，还在代码中对应的位置以注释形式记录了具体的参考来源及内容：

《你参考的资料说明》

3. 我独立完成了本次实验除以上方面之外的所有工作，包括代码与文档。 我清楚地知道，从以上方面获得的信息在一定程度上降低了实验难度，可能会影响起评分。

4. 我从未使用过他人的代码，不管是原封不动地复制，还是经过了某些等价转换。 我未曾也不会向他人（含此后各届同学）复制或公开我的实验代码，我有义务妥善保管好它们。 我提交至本实验的评测系统的代码，均无意于破坏或妨碍任何计算机系统的正常运转。 我清楚地知道，以上情况均为本课程纪律所禁止，若违反，对应的实验成绩将按“-100”分计。

# ch6课后习题
easy-fs中，root inode起到根目录的作用，所有实现的文件都需要经过root inode
如果root inode损坏，则文件不可访问
# ch7课后习题
linux中 cat 和 wc 统计文件的行数
cat file.txt | wc -l
比如这个文件就有78行
# ch6知识小节
## easy-fs
扁平化，只有根目录，无权限控制，无记录时间戳，无软硬链接，stat命令可以获取文件的具体信息
## 块设备
easy-fs与底层设备中通过BlockDevice链接，避免设备驱动的绑定（这里为了简便，采用松耦合），在底层驱动上，采用轮询方法
## block_cache块缓存区实现逻辑
若缓冲区找到，则返回
若找不到
1.缓存未达上限，创建一个
2.缓存达到上限，缓存替换
逻辑：因为类似队列结构（FIFO），所以从头部开始找引用计数为1的替换，若没找到引用计数为1，panic
## Stat
Stat 是一个用户态结构体，表示一个文件或目录的元数据。这个结构体通常在文件系统调用（如 fstat）中使用，以便将文件的状态信息从内核传递给用户空间应用程序。
对于state函数接受一个pub fn sys_fstat(fd: usize, st: *mut Stat) -> isize {
st参数，所以State变量初始化后存入了该位置SYSCALL_FSTAT => sys_fstat(args[0], args[1] as *mut Stat)
##　其他函数
alloc_inode，dealloc_data分配和回收块设备上面的编号（不是数据位图）
## ROOT_INODE，INODE，Direntry联系
如何根据ROOT_INODE来索引其他的目录项INODE
DirEntry来索引Inode，而不是Inode索引DirEntry
并且暴露给用户和应用程序的是DirEntry而不是Inode
DirEntry 是 独立于 inode 的数据结构，存储在 目录数据块中
DirEntry 主要是用来在目录中建立文件名与 inode 之间的映射关系
文件的 DirEntry 是唯一的，但可能在多个目录中
## link和unlink实现逻辑
硬链接，Inode id共享，所以只需要新增Direntry以及Inode即可，另外需要在磁盘Diskinode上面新增hard_link字段，来防止内存数据丢失
删除硬链接逻辑：将尾部的Direntry和想要删除的交换信息，后续size只需要减少即可逻辑上删除尾部的Direntry
# ch7知识小节
## 管道
管道是父子进程之间的通信机制（一种数据交换机制），可被视为一种特殊的内存文件，在fa_table中被管理
管道设计机制：环形的缓冲队列（Arc<Mutex<PiPeRingBuffer>>），具有读端以及写端，只有读端和写端都关闭后，管道资源被回收，PiPeRingBuffer::write_end维护弱引用计数，检查写端是否被全部关闭
写端与读端关系：仅有写端pipe缓存区会被耗尽，仅有读端可以（每次返回0也是可以的）
## 信号机制
信号机制补足单向事件通知机制，类似于硬件中断的软件级异步通知机制
1.信号接收方是一个进程：
处理方式：忽略，捕获（调用相关处理函数处理），终止
2.信号发送方有两个：
异步信号：进程正常执行时，内核或者其他进程发送了一个信号
同步信号：进程自身执行出发，处理Trap时内核将相应信号附加到进程控制块中，同步信号
## 信号产生：
信号两种：异步，同步
异步信号：
1.进程通过kill系统调用给自己或其他进程发信号
2.内核检测给某个进程发送信号，但事件与进程执行无关
同步信号
1.执行时触发了某种条件，在Trap到内核处理时，内核给进程发送相应信号，如SIFSEGV段错误和SIFILL非法指令
##　信号的三个处理函数
sys_sigaction：设置信号处理例程
sys_procmask：设置进程信号屏蔽掩码
sys_sigreturn：清除栈帧，从信号处理例程返回
需要在TaskControlBlock中添加signal_mask和signal_actions屏蔽其他信号量以及记录对于TCB中signals信号的相应
TaskControlBlockInner::signal_mask声明该进程内会屏蔽的信号，SignalAction::mask是回调函数会屏蔽的信号，二者需要做或操作
